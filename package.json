{
  "name": "Team09",
  "version": "0.0.0",
  "scripts": {
    "prestart": "npm install && bower install",
    "start": "gulp && gulp jshint && node server.js"
  },
  "dependencies": {
    "angular-resource": "^1.4.7"
  },
  "devDependencies": {
    "bower": "^1.5.2",
    "gulp": "^3.9.0",
    "gulp-inject": "^2.2.0",
    "gulp-jshint": "~1.11.2"
  }
}
